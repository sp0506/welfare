/**
 * 
 */
/*
var nameVar=/^[a-zA-Z\s-, ]{2,15}$/;
var emailVar= /^([\w-]+(?:\.[\w-]+)*)@((?:[\w-]+\.)*\w[\w-]{0,66})\.([a-z]{2,6}(?:\.[a-z]{2})?)$/i;
var userNameVar= /^[A-Za-z0-9_]{2,8}$/;
var passwordVar= /^[A-Za-z0-9!@#$%^&*()_]{6,111}$/;
var confirmVar= /^[A-Za-z0-9!@#$%^&*()_]{6,111}$/;
var locationVar= /^[A-Za-z]+$/;*/
/*function validate() {
	var checkPassword = form.password.value;
	var checkConfirm = form.confirm.value;*/
	/*var checkName = form.name.value;
	var checkEmail = form.email.value;
	var checkUserName = form.userName.value;
	var checkPassword = form.password.value;
	var checkConfirm = form.confirm.value;
	var checkLocation = form.location.value;
	if (!nameVar.test(checkName)) {
		document.getElementById('reg').innerHTML = "enter your name";
		
		form.name.focus();
		alert("enter name between 2 to 20 characters");
		return (false);
	}
	if (!userNameVar.test(checkUserName)) {
		document.getElementById('reg').innerHTML = "select valid username";
		
		form.userName.focus();
		//alert("choose username between 2 to 8 characters");
		return (false);
	}
	if (!emailVar.test(checkEmail)) {
		document.getElementById('confirm').innerHTML = "enter valid email ";
		
		form.email.focus();
		//alert("enter mail Eg: something@somemail.com");
		return (false);
	}*/
/*	if (!passwordVar.test(checkPassword)) {
		document.getElementById('reg').innerHTML = "enter your password ";
		
		form.password.focus();
		//alert("choose password between 6 to 10 characters");
		return (false);
	}
	if(checkPassword != checkConfirm){
		document.getElementById('reg').innerHTML = "password doesn't match ";
		form.confirm.focus();
		//alert("choose password between 6 to 10 characters");
		return (false);
	}*/
/*	
	if (!locationVar.test(checkLocation)) {
		document.getElementById('reg').innerHTML = "enter your city name";
		form.location.focus();
		return (false);
	}

	if ((document.form.role.value == 0)) {
		document.getElementById('reg').innerHTML = "role is required";
		// alert("xcvbnjm"+document.form.role.value);
		
		form.role.focus();
		alert("select from dropdown");
		return (false);
	}*/ 
/*	else {
		return (true);
	}
}*/